[tool.poetry]
name = "datajob-etl"
version = "0.1.0"
description = ""
authors = ["Harrison Zuleta Montoya <harrison.zmontoya@gmail.com>"]
readme = "README.md"
packages = [
    { include = "config" },
    { include = "utils" }
]

[tool.poetry.dependencies]
python = "^3.12"
sqlalchemy = "^2.0.48"
pydantic = "^2.12.5"
python-dotenv = "^1.2.2"
dbt = "^1.0.0"
pytest = "^9.0.2"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
